{
  "name": "task-manager-app",
  "version": "1.0.0",
  "private": true,
  "description": "Coordinator workspace to run backend and frontend together",
  "scripts": {
    "dev": "set NODE_OPTIONS=--no-deprecation && concurrently \"npm --prefix ./nodejs-mysql-backend run dev\" \"npm --prefix ./frontend run dev\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}